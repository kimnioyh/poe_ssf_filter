# PoE SSF Filter Customizer

Path of Exile **SSF(솔로 셀프파운드)** 유니크 수집용 아이템 필터 커스터마이저.
유니크 수집 현황 CSV를 올리면, **한 베이스타입의 유니크를 전부 모은 베이스는 `Hide`**,
**아직 안 모은 유니크가 남은 베이스는 `Show`** 하도록 NeverSink 필터를 자동 수정한다.

백엔드·로그인 없는 완전 클라이언트 사이드 정적 SPA (Vite + React + TS). 업로드한 파일은
브라우저 밖으로 나가지 않는다.

## 왜?

SSF에서는 중복 유니크가 판매 가치가 없다. 유니크는 미감정으로 떨어져 **베이스만 보이므로**,
그 베이스에서 나올 수 있는 유니크를 전부 모았다면 해당 베이스의 유니크 드랍은 더 볼 필요가 없다.
반대로 아직 못 모은 유니크가 남은 베이스는 엄격한 필터에서도 놓치지 않게 강조해서 보여준다.

## 사용법

1. **CSV 업로드** — **PoE ladder에 로그인해 export한 CSV** (`name, baseItem, category, owned, grouping …`).
   보유 여부는 `owned` 컬럼(`1` = 보유)으로 판별하므로, 실제 수집 현황이 반영되려면 반드시
   ladder에 로그인한 상태로 export해야 한다.
   형식 참고용 샘플: [`sample-uniques.csv`](./sample-uniques.csv) (전부 미보유 상태 → 모든 베이스가 "필요"로 표시, Show·드릴다운 시연용).
2. **NeverSink 필터 선택** — 내 `.filter` 업로드 또는 GitHub에서 strictness 프리셋 fetch.
3. **옵션 조정** — "드랍 가능 유형(grouping)" 토글로 완료 판정 기준을 조절.
   기본값은 몬스터 자연 드랍이 불가능한 `Vaal_Orb`·`Recipe`를 제외. 카테고리 단위 제외도 가능.
4. **다운로드** — 생성된 `Show`/`Hide` 블록을 원본 필터 맨 앞에 붙여 내려받는다.

### 완료 판정 규칙

`baseItem`으로 그룹핑해, **선택된 드랍 가능 유형에 속한 유니크가 전부 보유되면** 그 베이스는
완료(Hide). 드랍 불가로 꺼둔 유형의 미보유 유니크는 파밍 중 어차피 안 나오므로 판정에서 무시한다.

## 필터 수정 방식

NeverSink 내부 구조를 파싱하지 않는다. 필터는 first-match-wins이므로 두 블록을 **맨 앞에 prepend**만
한다 (완료/미완료 두 목록은 서로소). NeverSink 버전·리그가 바뀌어도 깨지지 않는다.

```
# SSF still-needed uniques (generated)
Show
    Rarity Unique
    BaseType == "Sage's Robe" ...
    SetBorderColor 255 200 0

# SSF collected-base hide (generated)
Hide
    Rarity Unique
    BaseType == "Onyx Amulet" ...
```

> 필터의 `BaseType`은 항상 **영어**다(게임 필터는 내부 영문명으로 매칭). 화면의 EN↔KO 토글은
> 표시 전용이며 필터 출력 언어에는 영향을 주지 않는다. (한글 이름 데이터는 `src/data/ko-names.ts`에
> 채우면 화면 표시가 전환된다.)

## 개발

```sh
npm install
npm run dev      # 개발 서버 (localhost:5173)
npm test         # 완료 판정 로직 self-check
npm run build    # 정적 빌드 -> dist/
```

## 라이선스

MIT

## 업데이트 내역

- **v0.20.0** — 미보유 유니크 드랍 알림 강화 토글(기본 ON). 강제 표시 Show 블록에 `MinimapIcon`·`PlayAlertSound`·`PlayEffect`·큰 폰트를 추가해 어지러운 화면에서도 드랍을 놓치지 않게.
- **v0.19.1** — 숨김 선택한 화폐·점술카드 이름에 취소선(회색) 표시로 숨김 상태를 더 명확히.
- **v0.19.0** — 점술카드 보상 그리드를 등급별(유니크/레어/매직/젬/화폐/점술/노말/기타) 접이식 그룹으로 정리(기본 접힘). 보상명에 인플루언스/변형 반영(예: 쉐이퍼 방패, 쇠락자 뼈 투구, 이중 영향 갑옷). 게임에서 비활성(삭제)된 카드 10장은 데이터에서 제외.
- **v0.18.0** — 점술카드 보상 카드 그리드. 카드별 보상 아이템·요구 스택·보상 등급(색)을 함께 표시하고, 카드를 클릭하면 숨김 토글. 보상 색은 등급 힌트일 뿐 실제 시세는 아님(SSF는 거래 시세 없음). 데이터에 `stack`·`cls`·`reward`·`rewardKo` 추가.
- **v0.17.0** — 점술카드 숨기기. 카드 이름(EN/KO 464종, poedb 스크랩)을 검색해 개별 선택하면 `Hide  Class == "Divination Card"  BaseType == …` 블록 생성. 데이터: `src/data/div-cards.json`, 스크립트: `scripts/fetch_divcards.py`.
- **v0.16.0** — 설정 자동 저장(localStorage)과 공유 링크(`#s=…`, 클립보드 복사). 컬렉션 탭에 수집 진행도 바·유니크 검색. 리그(메커니즘) 제외 토글로 완료 판정에서 특정 리그 유니크 무시. 최종 병합 필터 총 줄 수 표시.
- **v0.15.0** — 고유템 검색 강조 추가. 고유템 이름을 검색하면 그 베이스의 **고유템만** 강제 표시하는 별도 `Show` 블록(`Rarity Unique`, 노말/매직/레어 제외) 생성. 기존 베이스 강조와 독립.
- **v0.14.0** — Vercel Web Analytics 연동(방문자·페이지뷰, 쿠키 없음). *Vercel 대시보드에서 Analytics를 켜야 수집됨.*
- **v0.13.0** — 헤더에 건의하기 버튼(구글폼 링크, 새 탭). URL이 비어있으면 버튼 자동 숨김.
- **v0.12.0** — 화폐별 숨기기(스택 N개 이하 `StackSize <= N` 또는 통으로)와 잡화폐 일괄 선택, 상단 업데이트 내역 버튼(모달), 베이스 검색 드롭다운 바깥 클릭 시 닫힘.
- **v0.11.0** — 화면을 "필터 생성" / "보유 유니크" 탭으로 분리(라우터 없이). 컬렉션 탭에 모두/보유만 드롭다운 — "모두"는 미보유 유니크를 회색으로 함께 표시.
- **v0.10.0** — 베이스 강조에 최소/최대 아이템 레벨 조건(`ItemLevel >=/<=`, 값 있을 때만). "T0~T2 베이스는 항상 표시" 토글(기본 ON) — 고티어 유니크가 나올 수 있는 베이스는 다 모아도 숨기지 않음.
- **v0.9.0** — 베이스 아이템 검색 강조. EN/KO로 베이스타입을 검색해 선택하면 `Show` 강조 블록 생성(노말/매직/레어, `Corrupted False`, 시안 테두리+미니맵 아이콘). 유니크와 별개로 크래프팅 베이스 등을 강조.
- **v0.8.0** — "네버싱크가 숨기는 유니크만 강조" 옵션. 불러온 필터의 `Hide` 규칙을 읽어(수정 없음) 강제 Show 대상을 (미보유 ∩ 네버싱크가 숨기는 베이스)로 한정. Regular/Strict는 유니크를 안 숨겨 영향 없고, Uber-strict+에서만 강조.
- **v0.7.0** — 기본 언어 한국어, 보유 유니크를 카테고리별 카드 그리드(5열)로 큐레이션, 카테고리 한글화. 유니크 KO 이름에서 베이스 접미사 제거(1074개 정리).
- **v0.6.0** — 시작 시 전부-미보유 샘플 자동 로드 + 샘플 안내 배너, 동명 유니크 변종(Grand Spectrum 등) disambiguation 라벨로 개별 표시.
- **v0.5.0** — 드릴다운에 유니크 아이콘 + 한글명 표시.
- **v0.4.0** — 유니크 한글 이름·아이템 이미지 수집 (poedb `/kr/`, 이름 1158/1162·이미지 1162).
- **v0.3.0** — 베이스타입 한글 표시 데이터 병합 (`base_translations.json`, 542/543).
- **v0.2.0** — 필터 재적용 멱등성 가드(sentinel 마커) + 미완료 베이스 드릴다운.
- **v0.1.0** — 초기 구현: CSV → 완료 베이스 계산 → 네버싱크 필터에 Show/Hide 블록 prepend·다운로드.
